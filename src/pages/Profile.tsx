import { IonApp, IonTitle } from "@ionic/react";
import React from "react";

const Profile = () => {
  return (
    <IonApp>
      <IonTitle class="ion-text-center">Ini Profile</IonTitle>
    </IonApp>
  );
};

export default Profile;
